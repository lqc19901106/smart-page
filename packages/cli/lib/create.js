const path = require('path');
const childProcess = require('child_process');
const logger = require('../utils/log');


async function create(config){
    // 根据填写的项目生成 package.json

    // 生成编译环境配置

    // 生成项目目录

    // 安装项目的依赖
    process.exit(0);
}

module.exports = create;
